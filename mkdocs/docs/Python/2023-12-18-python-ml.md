---
layout: index
title: "Python ML"
category: Python
date: 2023-12-18 12:17:55
---

## Practise

- 排序

```python
# 对列表进行排序
arr=[[1,3], [4,1]]
arr.sort(key=lambda x: x[1]) #默认升序排序
arr.sort(key=lambda x: x[1], reverse=True) #按照降序排序

```

- 反向遍历list

```python
arr=list()
length = len(arr)

for i in range(1, length+1):
    print(arr[-i])
```

- 反转字符串

```python
s="abc"
s=[::-1]
```

- 使用heapp对dict进行排序
```python
import heapq
c_map = dict()

#对dict中的元素按照val排序，返回k大的keys
heapq.nlargest(k, c_map.keys(), key=lambda k:c_map[k])
```

## Python Machine Learning List

- NumPy
- Pandas
- Seaborn
- Matplotlib,
- Plotly
- Scikit-Learn
- Machine Learning
- Tensorflow


[python venv](https://docs.python.org/zh-cn/3/library/venv.html)

```bash
python -m venv ml
activate ml
# deactivate ml

pip install jupyterlab 
```