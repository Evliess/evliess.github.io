# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python release github docs

on:
  push:
    branches: [ "gh-pages" ]
  pull_request:
    branches: [ "gh-pages" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install mkdocs-material -U
        cd mkdocs
        pip install mkdocs-minify-plugin -U
        # mkdocs gh-deploy --config-file ../mkdocs/mkdocs.yml --remote-branch master
        mkdocs build
    - name: Commit and push to release branch
      run: |
        ls -la mkdocs/site
        # git config --global user.name 'github-actions[bot]'
        # git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        # git add .
        # git commit -m "chore: Auto deploy build assets" || echo "No changes to commit"
        # git push
